{### EXT Makefile ###}
test:
	cargo test
test-watch:
	fd -e rs | entr -r make test
{### EXT src/main.rs ###}

mod test {
    use super::*;
    #[test]
    fn basic_test() {
        let got = 1;
        let expect = 1;

        assert_eq!(got, expect);
    }
}

